name: colibri_stateless
description: Dart bindings for Colibri Stateless via FFI.
version: 0.1.0
repository: https://github.com/corpus-core/colibri-stateless
homepage: https://corpus-core.gitbook.io/specification-colibri-stateless

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  ffi: ^2.1.0
  http: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.29.0

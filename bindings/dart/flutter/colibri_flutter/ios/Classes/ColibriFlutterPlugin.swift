import Flutter
import UIKit

public class ColibriFlutterPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // Dart-only plugin. No method channels to register.
  }
}
